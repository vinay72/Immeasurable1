var emojis = require('emoji.json/emoji-compact.json')
var replace = require('.')

var out = {}
for (let emoji of emojis) {
	replace(emoji, function (code, match) {
		out[emoji] = code
	})
}

console.log(JSON.stringify(out))